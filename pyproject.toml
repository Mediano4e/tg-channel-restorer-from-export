[tool.poetry]
name = "tg-channel-restorer-from-export"
version = "0.1.0"
description = ""
authors = ["mediano4e"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
bs4 = "^0.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
